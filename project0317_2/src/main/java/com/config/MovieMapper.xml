<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MovieMapper">
	
	<select id="movieList" parameterType="string" resultType="MovieDTO">
		select mcode, mname, mename, mdirector, mactor, mgenre,
		mtime, mgrade,  to_char(mdate, 'yyyy/MM/dd') as mdate, mcontent, mimage, mpreview
		from movie
		where mcode = #{mcode}
	
	</select>
	
	
</mapper>